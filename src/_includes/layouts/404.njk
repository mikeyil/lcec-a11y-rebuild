---
layout: base.njk
---
<section class="page-title">
  <div class="container">
    <h1 class="page-title__heading text-uppercase">{{ page_title.heading }}</h1>
  </div>
</section>

<section class="section section--white">
  <div class="container text-center">
    <p class="section-lead">{{ body }}</p>
    <a href="{{ '/' | url }}" class="btn btn--outline">{{ button_text }}</a>
    <a href="{{ '/sitemap.xml' | url }}" class="btn btn--outline">View Sitemap</a>
  </div>
</section>