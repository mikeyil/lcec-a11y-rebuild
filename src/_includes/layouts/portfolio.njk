---
layout: base.njk
---
<section class="section section--blue">
  <div class="container">
    <div class="page-intro">
      <h1 class="page-intro__heading text-uppercase">{{ page_title.heading }}</h1>
      <p>{{ intro_body | safe }}</p>
      <a href="{{ '/contact/' | url }}" class="btn btn--ghost">Contact Us</a>
    </div>
  </div>
</section>

<section class="section section--white section--flush-bottom">
  <div class="container">
    <h2 class="content-blocks__heading text-uppercase">{{ projects_heading }}</h2>
    <div class="content-blocks__grid">
      {% for project in projects %}
      <article class="content-blocks__block content-blocks__block--flush">
        {% if project.image %}
        <picture>
          <source srcset="{{ project.image | toWebP | url }}" type="image/webp" />
          <img src="{{ project.image | url }}" alt="" class="content-blocks__image" width="600" height="400" aria-hidden="true" loading="lazy" />
        </picture>
        {% endif %}
        <p>{{ project.title }}</p>
      </article>
      {% endfor %}
    </div>
  </div>
</section>

{% include "partials/social-section.njk" %}

{% include "partials/cta.njk" %}
