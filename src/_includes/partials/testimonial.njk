<section class="section testimonial--{{ variant | default('dark') }}{% if section_class %} {{ section_class }}{% endif %}">
  <div class="container">
    {% if testimonial_heading %}
    <h2 class="section-heading">{{ testimonial_heading }}</h2>
    {% endif %}
    {% set _q = testimonials.items[0] %}
    <blockquote class="testimonial">
      <p class="testimonial__text">&ldquo;{{ _q.short if quote_short else _q.text }}&rdquo;</p>
      <footer class="testimonial__attribution">&mdash; {{ _q.attribution }}</footer>
    </blockquote>
  </div>
</section>
